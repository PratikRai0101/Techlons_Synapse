import { Text, View } from 'react-native';

export default function DoctorProfileScreen() {
  return (
    <View className="flex-1 bg-white">
      <Text className="text-2xl font-bold p-4">Profile (Doctor)</Text>
      {/* Add the profile UI here */}
    </View>
  );
}
