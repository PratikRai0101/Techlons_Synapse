import { Text, View } from 'react-native';

export default function DoctorAuditScreen() {
  return (
    <View className="flex-1 bg-white">
      <Text className="text-2xl font-bold p-4">Audit (Doctor)</Text>
      {/* Add the audit UI here */}
    </View>
  );
}
