import { Text, View } from 'react-native';

export default function DoctorReportsScreen() {
  return (
    <View className="flex-1 bg-white">
      <Text className="text-2xl font-bold p-4">Reports (Doctor)</Text>
      {/* Add the reports UI here */}
    </View>
  );
}
