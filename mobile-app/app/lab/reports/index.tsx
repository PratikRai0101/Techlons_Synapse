import { Text, View } from 'react-native';

export default function LabReportsScreen() {
  return (
    <View className="flex-1 bg-white">
      <Text className="text-2xl font-bold p-4">Reports (Lab)</Text>
      {/* Add the reports UI here */}
    </View>
  );
}
